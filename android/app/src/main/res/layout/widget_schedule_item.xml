<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:paddingLeft="12dp"
    android:paddingRight="12dp">

    <!-- Timeline Stem (Vertical line) -->
    <FrameLayout
        android:id="@+id/timeline_stem"
        android:layout_width="2dp"
        android:layout_height="match_parent"
        android:layout_marginLeft="50dp"
        android:background="#20FFFFFF" />

    <!-- Time Label -->
    <TextView
        android:id="@+id/item_time_label"
        android:layout_width="45dp"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:text="09:00"
        android:textColor="#80FFFFFF"
        android:textSize="12sp"
        android:gravity="right" />

    <!-- Event Container -->
    <LinearLayout
        android:id="@+id/item_event_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toRightOf="@id/timeline_stem"
        android:layout_marginLeft="12dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:background="@drawable/widget_event_background"
        android:padding="8dp"
        android:visibility="invisible">

        <TextView
            android:id="@+id/item_event_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Event Title"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/item_event_duration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="45 mins"
            android:textColor="#80FFFFFF"
            android:textSize="10sp" />
    </LinearLayout>

</RelativeLayout>
